pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = 'wiremock-generating-gradle-plugin'

include 'integration-test-api'
include 'integration-test'
include 'plugin'
